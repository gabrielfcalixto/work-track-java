spring.application.name=project-manager

# Configurações pra usar o banco de dados PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/projeto
# spring.datasource.username=postgres
# spring.datasource.password=ROOT
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configurações para usar o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/projeto?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configurações do Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true

# Desabilita a execução automática de data.sql
spring.sql.init.mode=always

# Configurações do JWT
projeto.jwtSecret=bxOksa8BHgdAhR80Y3pEYvS5M+MnF2sheFDqprkTqQ4odqoszJLW1ikw64/nT/dTvlgrcBTq7HfK1B9Gai2h5A==
projeto.jwtExpirationMs=9000000

# Configurações do servidor de e-mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gabrielsuporteremoto@gmail.com
spring.mail.password=huxi ywzn ugmn bfgp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuração global para exibir apenas logs de erro
# logging.level.root=ERROR
# Definição específica para frameworks do Spring e Hibernate
# logging.level.org.springframework=ERROR
# logging.level.org.hibernate=ERROR

# Caso queira ver apenas SQL gerado e não as ligações de parâmetros, remova a linha TRACE
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE